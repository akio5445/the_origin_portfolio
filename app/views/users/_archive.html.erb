<!-- 月別アーカイブ -->
<div class="archive_index_side">
  <h4 class="font-italic">過去ログ</h4>
  <ul>
    <% archives.each do |yyyymm, count| %>
      <li><%= link_to ymconv(yyyymm, count.to_s), user_archive_path(@user, yyyymm) %></li>
    <% end %>
  </ul>
</div>
