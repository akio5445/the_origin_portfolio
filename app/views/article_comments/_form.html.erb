<%= form_with(model: [article, article_comment] ) do |form| %>
  <div class="">
    <%= form.text_area :content, class: "article_comment_area" %>
  </div>
  <div class="actions">
    <%= form.submit "Comment", class: "button" %>
  </div>
<% end %>